extends layout

block content
 
 h1 #{title}
 form(method="POST" action="/articles/add")
  label(for="tit") Title
  input(type="text" name="title" id="tit" class="form-control" placeholder="..." required)
  label(for="texta") Body
  textarea(id="texta" class="form-control" name="body" required)
  <br>
  input(type="submit" value="submit" class="btn btn-primary")
   